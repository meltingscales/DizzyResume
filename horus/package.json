{
  "name": "@dizzy-resume/horus",
  "version": "0.1.0",
  "private": true,
  "description": "Horus — DizzyResume browser extension",
  "scripts": {
    "dev": "vite build --watch & vite build --config vite.content.config.ts --watch",
    "build": "vite build && vite build --config vite.content.config.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.325",
    "typescript": "^5.8.0",
    "vite": "^6.0.0"
  }
}
